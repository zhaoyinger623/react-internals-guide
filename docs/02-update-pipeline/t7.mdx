---
title: Hooks：读取与计算状态
description: React 原理全景中的「Hooks：读取与计算状态」，从运行机制继续深入源码。
---

import { TutorialSequence } from '../../theme/TutorialSequence';

<div className="tutorial-chapter">

阶段三 · Render / 10

**🧭 主线**setState→Update→Lane→Scheduler→Render→Effect→Commit→DOM

先想清楚 Hooks 到底解决什么问题,再谈底层。你已经知道([第一层·组件渲染](/01-foundations/t1)):**函数组件每次渲染都从头到尾重新执行一遍**。那么——

**灵魂拷问:**函数一跑完,里面的局部变量就都销毁了,可为什么下次渲染时 `useState` 的 `count` **还在、还记得上次的值**?

**答案:**React 把状态存在函数**外面**——存在这个组件对应的 `Fiber` 节点上。所谓 **Hook,就是「在 Fiber 上开一块记忆、并把它和你的每次调用对应起来」的机制**。整章就回答三个问题:

1.  **存在哪?** 状态放在 Fiber 的哪个位置(下面 ①)
2.  **怎么对应上?** 重新渲染时,React 怎么知道哪个值属于哪个 Hook(② ③)
3.  **每种 Hook 具体存什么?** useState / useEffect / useMemo 各放什么(④)

## ① 存在哪:Fiber 上的一排「储物柜」

把 Fiber 想成一排编号的**储物柜**:你在组件里调用的**第 1、2、3 个 Hook,就对应 1 号、2 号、3 号柜子**。技术上,这排柜子是挂在 Fiber 的 `memoizedState` 字段上的一条**单链表**,每个 Hook 是一个节点,靠 `next` 串起来:

```js
// 每个 hook 节点
{
  memoizedState,  // 该 hook 存的值(state / effect / memo 值…)
  baseState,      // 并发中断时的「存档起点」
  baseQueue,      // 被跳过、留待下次的更新
  queue,          // 更新队列(useState / useReducer)
  next,           // → 下一个 hook
}
fiber.memoizedState → hook1 → hook2 → hook3 → null
```

<div className="tutorial-visual">
<svg viewBox="0 0 760 110" style={{ width: "100%", height: "auto", margin: "6px 0" }}><defs><marker id="ha" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto"><path d="M0,0 L6,3 L0,6 Z" style={{ fill: "var(--fiber)" }} /></marker></defs><g fontFamily="monospace" fontSize="11"><rect x="8" y="35" width="120" height="46" rx="10" style={{ fill: "var(--panel2)", stroke: "var(--accent)" }} strokeWidth="2" /><text x="68" y="53" textAnchor="middle" style={{ fill: "var(--accent)" }} fontSize="11">fiber</text> <text x="68" y="70" textAnchor="middle" style={{ fill: "var(--dim)" }} fontSize="9.5">.memoizedState</text> <g style={{ animation: "pulse 2s infinite" }}><rect x="175" y="35" width="150" height="46" rx="10" style={{ fill: "var(--panel2)", stroke: "var(--fiber)" }} strokeWidth="2" /><text x="250" y="52" textAnchor="middle" style={{ fill: "var(--fiber)" }} fontWeight="700">hook1</text> <text x="250" y="70" textAnchor="middle" style={{ fill: "var(--dim)" }} fontSize="9.5">useState(count)=0</text></g> <rect x="372" y="35" width="150" height="46" rx="10" style={{ fill: "var(--panel2)", stroke: "var(--fiber)" }} strokeWidth="2" /><text x="447" y="52" textAnchor="middle" style={{ fill: "var(--fiber)" }} fontWeight="700">hook2</text> <text x="447" y="70" textAnchor="middle" style={{ fill: "var(--dim)" }} fontSize="9.5">useState(name)</text> <rect x="569" y="35" width="150" height="46" rx="10" style={{ fill: "var(--panel2)", stroke: "var(--fiber)" }} strokeWidth="2" /><text x="644" y="52" textAnchor="middle" style={{ fill: "var(--fiber)" }} fontWeight="700">hook3</text> <text x="644" y="70" textAnchor="middle" style={{ fill: "var(--dim)" }} fontSize="9.5">useEffect</text> <line x1="130" y1="58" x2="172" y2="58" style={{ stroke: "var(--fiber)" }} strokeWidth="2" markerEnd="url(#ha)" /><line x1="327" y1="58" x2="369" y2="58" style={{ stroke: "var(--fiber)" }} strokeWidth="2" markerEnd="url(#ha)" /><line x1="524" y1="58" x2="566" y2="58" style={{ stroke: "var(--fiber)" }} strokeWidth="2" markerEnd="url(#ha)" /><text x="250" y="98" textAnchor="middle" style={{ fill: "var(--dim)" }} fontSize="9.5">next</text> <text x="447" y="98" textAnchor="middle" style={{ fill: "var(--dim)" }} fontSize="9.5">next</text> <text x="644" y="98" textAnchor="middle" style={{ fill: "var(--dim)" }} fontSize="9.5">next → null</text></g></svg>
</div>

## ② 怎么对应上:只认「第几个」,所以不能条件调用

存进链表了,那 React 重新渲染时怎么把值对回每个 Hook?关键点:**它不看 Hook 叫什么名字,只数「你调用的第几个」**——第 1 个调用拿 1 号柜、第 2 个拿 2 号柜……首次渲染按顺序建柜子,以后每次渲染按**同样的顺序**挨个取。

所以一旦你 `if(x) useState()` 少调了一个,后面所有 Hook 的「第几个」就**全部错位一格**,值张冠李戴。**铁律:每次渲染,Hook 的数量和顺序必须完全一致**(不能放进 if / for / return 之后)。下面这个演示把「按位置对应」画出来了:

### 互动:顺序调用 vs 条件跳过

**怎么操作：**先点“正常顺序渲染”，观察每个 Hook 调用与右侧槽位一一对应；再点“条件跳过”，从第二个调用开始查看错位。

## ③ 同一个 useState，为什么首次和更新走不同代码

组件代码里始终写的是 `useState(initialState)`，但 React 内部需要处理两种不同情况：

**首次 Render：还没有 Hook 节点**

调用 `mountState`，创建 Hook 节点，保存初始 state 和更新队列，并把节点接到当前 Fiber 的 Hook 链表。

**更新 Render：Hook 节点已经存在**

调用 `updateState`，按调用顺序取得对应 Hook，处理它队列中的 update，计算本轮 state。

公开的 `useState` 函数并不知道当前是首次还是更新。函数组件开始 Render 时，`renderWithHooks` 会先把当前实现表设置为 mount 版或 update 版；随后 `useState` 从这张表中调用对应函数。这张“当前实现表”就是 dispatcher。

**组件执行到 `useState()`**

↓ 读取当前 dispatcher 中的 `useState` 实现

**首次 Render → mountState**
创建 Hook，保存初始值

**更新 Render → updateState**
取得旧 Hook，处理更新队列

对应到源码，就是在执行函数组件之前选择一套实现，然后让公开 API 转发过去：

```js
// renderWithHooks:每次渲染函数组件前,按 mount/update 换 dispatcher
ReactCurrentDispatcher.current =
  current === null || current.memoizedState === null
    ? HooksDispatcherOnMount     // 首次:mountState / mountEffect …(创建 hook)
    : HooksDispatcherOnUpdate;   // 更新:updateState / updateEffect …(复用 hook)

// useState 转发到当前 dispatcher
function useState(initial){
  return resolveDispatcher().useState(initial);
}
```

**为什么组件外调用 Hook 会报错？**因为组件外没有正在执行的 `renderWithHooks`，也就没有可供本次 Render 使用的 Hook dispatcher。React 无法知道应该把 Hook 节点挂到哪个 Fiber。

## ④ 各 Hook 具体存什么 · useState / useReducer

前面讲的是通用机制(链表、按序对应)。现在看每种 Hook 的「柜子」里到底放了什么。

`useState` 复用 `useReducer` 的更新流程，并使用内置的 state reducer（`(s,a)=>typeof a==='function'?a(s):a`）。dispatch 时，update 进入该 Hook 的 `queue.pending` 环状链表（详见[Trigger·UpdateQueue](/02-update-pipeline/t9)）；渲染时从 `baseState` 开始依次处理 action，得到新的 `memoizedState`。

```js
// dispatchSetState 里的「提前计算」优化(eager state bailout)
if(fiber.lanes === NoLanes){          // 队列里没有别的待处理更新时
  const eager = reducer(currentState, action); // 先算出新 state
  update.hasEagerState = true; update.eagerState = eager;
  if(is(eager, currentState)) return;  // 新旧相等 → 直接不调度渲染!
}
scheduleUpdateOnFiber(fiber, lane);   // 不相等才真正安排渲染
```

**这就是「`setCount(1)` 时 count 已经是 1 就不重渲染」的底层原因**:dispatch 时先用 reducer 试算,若结果和当前 state `Object.is` 相等,连渲染都不安排(注意:若是父级传下的其它更新导致重渲染,该组件仍可能渲染)。

## ④ 各 Hook 具体存什么 · useEffect(副作用)

`useEffect` 的 hook 节点里存一个 effect 对象 `&#123;tag, create, destroy, deps, next&#125;`,所有 effect 再单独串成一条**环状 effect 链表**挂在 fiber 上。Render 阶段对比 deps 决定是否给 fiber 打 `Passive` flag;Commit 后异步遍历这条链表执行(详见[Render·变更标记](/02-update-pipeline/t10))。

## ④ 各 Hook 具体存什么 · useMemo / useCallback(缓存)

```js
// useMemo 简化:hook.memoizedState = [value, deps]
function useMemo(create, deps){
  const hook = updateWorkInProgressHook();
  const prev = hook.memoizedState;
  if(prev !== null && deps !== null && areHookInputsEqual(deps, prev[1]))
    return prev[0];              // deps 没变 → 直接返回缓存值
  const next = create();          // 变了 → 重算
  hook.memoizedState = [next, deps];
  return next;
}
// useCallback 只是把函数本身当值缓存:hook.memoizedState=[fn,deps]
```

## 小结:每种 Hook 的柜子里放了什么

Hook

它的 memoizedState(柜子)里存的东西

`useState` / `useReducer`

当前 state 值 + 一个更新队列 queue

`useEffect` / `useLayoutEffect`

一个 effect 对象 &#123;create, destroy, deps, tag&#125;

`useMemo`

\[缓存的计算结果, deps\]

`useCallback`

\[缓存的函数, deps\]

`useRef`

一个 &#123; current &#125; 对象(见 [补充·useRef](/05-core-topics/ref))

`useContext`

不占柜子(readContext 不走 hook 链表,只登记依赖)

**Render 中的状态计算完成：**Hook 按调用顺序挂在 Fiber 上；组件执行时，React 读取 Hook 链表并处理更新队列，得到本次状态。下一章进入 [Diff](/02-update-pipeline/t6)，比较组件返回的新旧子节点。

<TutorialSequence kind="hooks" />

</div>
