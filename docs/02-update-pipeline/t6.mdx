---
title: Diff：协调子节点
description: React 原理全景中的「Diff：协调子节点」，从运行机制继续深入源码。
---

import { TutorialSequence } from '../../theme/TutorialSequence';

<div className="tutorial-chapter">

阶段三 · Render / 11

**🧭 主线**setState→Update→Lane→Scheduler→Render→Effect→Commit→DOM

**🗺 本章路线图**

Diff 就是「拿**新描述**去更新/复用**旧 fiber**」;靠三大假设把最优算法的 O(n³) 砍到 **O(n)**。

本章包括：**三大假设 → 单节点 Diff → 多节点两轮遍历 → lastPlacedIndex 移动判断 → index 当 key 为何出 bug**。

对比两棵任意树的最优算法是 **O(n³)**(n 是节点数)——对上千节点的页面完全不可用。React 放弃「找到最优最少操作」,转而基于三条**符合前端实际情况的假设(启发式策略)**,把复杂度砍到 **O(n)**。理解这三条假设,就理解了 React Diff 的一切行为(包括各种「诡异」的复用/重建)。

## 三大假设(Diff 的地基,务必吃透)

### 🧮 算法复杂度优化

React 通过三大启发式假设,把 Diff 从理论上的 **O(n³)** 降到 **O(n)**——用线性时间复杂度实现高效的虚拟 DOM 比对。

<div className="tutorial-visual">
<svg viewBox="0 0 420 280" style={{ width: "100%", height: "auto" }} fontFamily="ui-monospace,monospace"><text x="18" y="140" transform="rotate(-90 18 140)" textAnchor="middle" style={{ fill: "var(--dim)" }} fontSize="11">相对计算量</text> <line x1="60" y1="30" x2="60" y2="240" style={{ stroke: "var(--line)" }} strokeWidth="1.5" /><line x1="60" y1="240" x2="405" y2="240" style={{ stroke: "var(--line)" }} strokeWidth="1.5" /><line x1="60" y1="45" x2="405" y2="45" style={{ stroke: "var(--line)" }} strokeWidth="1" strokeDasharray="3 4" opacity=".5" /><text x="54" y="49" textAnchor="end" style={{ fill: "var(--dim)" }} fontSize="9">O(n³)</text> <text x="54" y="244" textAnchor="end" style={{ fill: "var(--dim)" }} fontSize="9">0</text> <rect x="110" y="45" width="90" height="195" rx="4" style={{ fill: "rgba(255,107,107,.5)", stroke: "var(--warn)" }} strokeWidth="1.5" /><text x="155" y="258" textAnchor="middle" style={{ fill: "var(--txt2)" }} fontSize="10">理论算法 O(n³)</text> <rect x="270" y="233" width="90" height="7" rx="3" style={{ fill: "var(--recon)", stroke: "var(--recon)" }} strokeWidth="1.5" /><text x="315" y="258" textAnchor="middle" style={{ fill: "var(--txt2)" }} fontSize="10">React Diff O(n)</text> <text x="315" y="224" textAnchor="middle" style={{ fill: "var(--recon)" }} fontSize="9" fontWeight="700">≈ 一根线</text></svg>
</div>

### 💡 三大核心假设

1

**跨层级节点移动极少发生**

只在同一父容器的直接子节点范围内做 Diff(跨层就删了重建)

2

**不同 type 的节点生成不同的树**

type 不同则跳过子树比对,直接卸载旧的、重建新的

3

**同层子节点用 key 标识稳定身份**

通过 key 建立「节点 ↔ 数据」的稳定关联,才能移动复用

下面逐条展开(每条都有代码示例 + 实战陷阱):

### ①只比同层级,不跨层移动 Tree Diff

React **只在同一层级、同一父节点下**对比新旧节点,**绝不跨层级比较或移动**。如果一个节点在更新后「换了父亲/换了层级」,React 不会去「找到它并搬过去」,而是:**在旧位置删除它、在新位置重新创建**。

```js
// 更新前                    更新后(把 A 从顶层挪进 B 里)
<div><A/><B/></div>           <div><B><A/></B></div>
// React 不会「移动」A,而是:删掉顶层的 A、在 B 里新建一个 A
// → A 的 state 丢失、DOM 重建。所以:别用跨层级移动来做动画/结构变化
```

为什么敢这么假设?因为 Web UI 里**跨层级移动节点极少见**。为了这种罕见情况做「全树查找匹配」不划算,直接删了重建,换来 O(n) 的同层遍历。

### ②type 不同,整棵子树直接换掉 Component Diff

对比同一位置的新旧节点时,只要 **type 不一样**(`div`→`span`、组件 `Foo`→`Bar`),React **立即认定整棵子树都变了**:**卸载旧子树(触发 unmount / componentWillUnmount、清理 effect)、从零构建新子树**,不再深入对比里面的子节点。只有 **type 相同**,才保留该节点、只更新变化的属性,并继续 Diff 它的孩子。

```js
// type 相同 → 复用节点,只 patch 属性,继续 diff 子树
<div className="a">  →  <div className="b">   ✅ 复用 div,只改 class

// type 不同 → 整棵子树销毁重建(哪怕内部长得几乎一样)
<div>…大量子节点…</div>  →  <section>…相同子节点…</section>
// div≠section → 里面的子节点全部卸载重建,state/DOM 全丢
```

**实战陷阱:**条件渲染时若外层 type 变了(如 `&#123;cond ? &lt;div&gt;… : &lt;section&gt;…&#125;`),里面组件会被**卸载重挂载**、state 丢失。想保留状态,就保持 type 一致(用同一标签 + 条件改属性),或用 key 控制。

### ③用 key 给同层子节点一个稳定「身份证」 Element Diff

对**同层的一组子节点**(列表),React 靠 `key` 判断「新旧两轮里,哪个是同一个节点」。有稳定 key,即使节点在列表里**换了位置**,React 也能认出它、**移动并复用**(保留 state 和 DOM),而不是重建。没有 key(或用会变的 index),React 只能**按位置**硬匹配,顺序一变就全判错。

```js
// 有稳定 key:React 认得出 b 只是移到了前面 → 移动复用
[a,b,c]  →  [b,a,c]     key=id:只移动,不重建 ✅

// 无 key / index 当 key:按位置比 → 以为 位置0 从 a 变成了 b …全部误判
```

**三项前提：**① **只看同层**(跨层就删了重建)→ ② **type 变就整棵换**(type 同才复用)→ ③ **同层列表靠 key 认身份**(有稳定 key 才能移动复用)。下面「单节点 / 多节点 Diff」就是这三条假设的具体落地。

## 单节点 Diff(reconcileSingleElement)

更新后只有**一个**子节点(`&lt;div&gt;&#123;x&#125;&lt;/div&gt;`)时,React 拿这个新元素去和**旧的一串子 fiber**逐个比:

```js
function reconcileSingleElement(returnFiber, currentFirstChild, element){
  let child = currentFirstChild;
  while(child !== null){
    if(child.key === element.key){          // ① 先比 key
      if(child.type === element.type){        // ② key 同,再比 type
        deleteRemainingChildren(returnFiber, child.sibling); // 其余兄弟全删
        const existing = useFiber(child, element.props); // ★复用!
        return existing;
      }
      deleteRemainingChildren(returnFiber, child); // key 同 type 不同 → 从它起全删
      break;
    } else {
      deleteChild(returnFiber, child);        // key 不同 → 删这一个,继续看兄弟
    }
    child = child.sibling;
  }
  return createFiberFromElement(element);    // 没匹配到 → 新建
}
```

注意顺序:**key 相同但 type 不同**时,不仅删自己,还 `deleteRemainingChildren` 把后面兄弟一起删(因为 key 唯一,后面不可能再有同 key 的);**key 不同**只删当前继续找。这正是假设 ②③ 的落地。

## 多节点 Diff:两轮遍历(reconcileChildrenArray)

**先不要看源码，固定一个例子：**旧列表是 `[A, B, C, D]`，新列表是 `[A, C, E, B]`。字母就是稳定的 key，元素 type 都相同。

### 第一轮：只比较同一个下标，直到第一次对不上

新下标

旧节点

新节点

结果

0

A

A

key/type 相同，复用 A；继续

1

B

C

key 不同，**第一轮立即停止**

**停止时的现场：**A 已处理；旧节点还剩 `[B,C,D]`；新节点还剩 `[C,E,B]`。React 不会继续拿旧 C 和新 E 做同位比较，而是转入第二轮。

### 第二轮：旧节点建 Map，新节点按 key 查找

先把剩余旧节点建成：`&#123; B → oldB, C → oldC, D → oldD &#125;`。

依次处理的新节点

去 Map 查找

React 做什么

Map 剩余

C

找到 oldC

复用 C；从 Map 删除 C

B、D

E

找不到

创建新 Fiber E，并标记 Placement

B、D

B

找到 oldB

复用 B；根据旧位置判断是否移动

D

新数组结束

Map 仍有 D

旧 D 没被使用，标记删除

空

**两轮不是把整棵列表重复比较两遍。**第一轮只快速吃掉开头连续对得上的部分；一旦错位，第二轮只处理“双方剩下的部分”，用 Map 避免反复扫描旧列表。

更新后是**一个数组**子节点(列表)时最复杂。React 用**两轮遍历**(不是 Vue 的严格双端 Diff,但吸收了同类思想):

-   **第一轮:**新旧数组从左到右同位置比较,key+type 相同就复用,一旦不同**立即跳出**。这一轮吃掉最常见的「尾部追加 / 只改内容不换位」。
-   **第二轮:**把剩余旧节点存进 `Map(key → fiber)`,遍历剩余新节点用 key 去 Map 里捞。捞到 = **复用**(可能要移动);捞不到 = **新建**;最后 Map 里没被捞走的 = **删除**。

```js
// reconcileChildrenArray 骨架(极简)
let lastPlacedIndex = 0, newIdx = 0, oldFiber = currentFirstChild;
// —— 第一轮:同位比较 ——
for(; oldFiber !== null && newIdx < newChildren.length; newIdx++){
  const newFiber = updateSlot(oldFiber, newChildren[newIdx]); // key 不同则返回 null
  if(newFiber === null) break;                // ★一旦对不上,跳出第一轮
  lastPlacedIndex = placeChild(newFiber, lastPlacedIndex, newIdx);
  oldFiber = oldFiber.sibling;
}
if(newIdx === newChildren.length){ deleteRemainingChildren(oldFiber); return; } // 新的排完 → 旧的剩下全删
if(oldFiber === null){ /* 旧的用完 → 剩下新的全部新建 */ }
// —— 第二轮:剩余旧节点建 Map,新节点来捞 ——
const existingChildren = mapRemainingChildren(oldFiber);  // key → fiber
for(; newIdx < newChildren.length; newIdx++){
  const newFiber = updateFromMap(existingChildren, newChildren[newIdx]);
  lastPlacedIndex = placeChild(newFiber, lastPlacedIndex, newIdx);
  if(newFiber.alternate) existingChildren.delete(newFiber.key); // 捞走的从 Map 移除
}
existingChildren.forEach(c => deleteChild(c));  // Map 里剩的 = 删除
```

### 📊 两轮遍历流程总览

<div className="tutorial-visual">
<svg viewBox="0 0 1000 300" style={{ width: "100%", height: "auto" }} fontFamily="ui-monospace,monospace"><defs><marker id="dfArr" markerWidth="9" markerHeight="9" refX="7" refY="3" orient="auto"><path d="M0,0 L7,3 L0,6 Z" style={{ fill: "var(--accent)" }} /></marker></defs><rect x="20" y="30" width="300" height="230" rx="12" style={{ fill: "rgba(126,231,135,.10)", stroke: "var(--recon)" }} strokeWidth="1.5" /><text x="170" y="55" textAnchor="middle" style={{ fill: "var(--recon)" }} fontSize="13" fontWeight="700">第一轮:从左到右同位比较</text> <rect x="45" y="75" width="250" height="40" rx="7" style={{ fill: "var(--panel)", stroke: "var(--line)" }} /><text x="170" y="100" textAnchor="middle" style={{ fill: "var(--txt)" }} fontSize="11.5">新旧同位置:key+type 相同?</text> <rect x="45" y="130" width="250" height="40" rx="7" style={{ fill: "rgba(97,218,251,.14)", stroke: "var(--accent)" }} /><text x="170" y="155" textAnchor="middle" style={{ fill: "var(--txt)" }} fontSize="11.5">相同 → 复用旧 fiber(改 props)</text> <rect x="45" y="185" width="250" height="50" rx="7" style={{ fill: "rgba(255,123,156,.14)", stroke: "var(--render)" }} /><text x="170" y="206" textAnchor="middle" style={{ fill: "var(--txt)" }} fontSize="11.5">一旦不同 → 立即跳出第一轮</text><text x="170" y="223" textAnchor="middle" style={{ fill: "var(--dim)" }} fontSize="10">(覆盖最常见的"尾部追加")</text> <line x1="325" y1="145" x2="375" y2="145" style={{ stroke: "var(--accent)" }} strokeWidth="2.5" markerEnd="url(#dfArr)" /><text x="350" y="135" textAnchor="middle" style={{ fill: "var(--dim)" }} fontSize="10">还有剩余</text> <rect x="380" y="30" width="600" height="230" rx="12" style={{ fill: "rgba(242,184,75,.10)", stroke: "var(--sched)" }} strokeWidth="1.5" /><text x="680" y="55" textAnchor="middle" style={{ fill: "var(--sched)" }} fontSize="13" fontWeight="700">第二轮:剩余旧节点建 Map(key→fiber)</text> <rect x="405" y="75" width="550" height="36" rx="7" style={{ fill: "var(--panel)", stroke: "var(--line)" }} /><text x="680" y="98" textAnchor="middle" style={{ fill: "var(--txt)" }} fontSize="11.5">遍历剩余新节点,用 key 去 Map 里捞</text> <rect x="405" y="123" width="175" height="55" rx="7" style={{ fill: "rgba(97,218,251,.14)", stroke: "var(--accent)" }} /><text x="492" y="145" textAnchor="middle" style={{ fill: "var(--txt)" }} fontSize="11">捞到 → 复用/移动</text><text x="492" y="162" textAnchor="middle" style={{ fill: "var(--dim)" }} fontSize="9.5">lastPlacedIndex 判是否右移</text> <rect x="592" y="123" width="175" height="55" rx="7" style={{ fill: "rgba(126,231,135,.14)", stroke: "var(--recon)" }} /><text x="680" y="150" textAnchor="middle" style={{ fill: "var(--txt)" }} fontSize="11">捞不到 → 新建</text> <rect x="779" y="123" width="175" height="55" rx="7" style={{ fill: "rgba(255,123,156,.14)", stroke: "var(--render)" }} /><text x="866" y="145" textAnchor="middle" style={{ fill: "var(--txt)" }} fontSize="11">Map 里剩下的</text><text x="866" y="162" textAnchor="middle" style={{ fill: "var(--dim)" }} fontSize="9.5">→ 删除</text> <rect x="405" y="190" width="550" height="45" rx="7" style={{ fill: "none", stroke: "var(--line)" }} strokeDasharray="4 4" /><text x="680" y="217" textAnchor="middle" style={{ fill: "var(--dim)" }} fontSize="11">key 稳定 = 身份稳定 → 能"移动"而非"重建";index 当 key 会让身份错乱</text></svg>
</div>

## lastPlacedIndex:决定「移动还是不动」的灵魂

`lastPlacedIndex` 记录“目前已处理的可复用节点中，旧位置最靠右的下标”。每处理一个新节点，`placeChild` 用它判断该节点是否需要移动：

```js
function placeChild(newFiber, lastPlacedIndex, newIndex){
  newFiber.index = newIndex;
  const current = newFiber.alternate;      // 复用的旧 fiber(新建则为 null)
  if(current !== null){
    const oldIndex = current.index;        // 它在旧列表里的原位置
    if(oldIndex < lastPlacedIndex){         // ★原来在左边,现在要在右边 → 得移动
      newFiber.flags |= Placement;         // 标记「移动」
      return lastPlacedIndex;
    } else {
      return oldIndex;                     // 原来就靠右 → 不用动,抬高基准线
    }
  } else {
    newFiber.flags |= Placement;           // 新建也是 Placement(插入)
    return lastPlacedIndex;
  }
}
```

要点：**只有当一个可复用节点「原来的位置比基准线还靠左」时,才判定它要移动**;否则不动、并把基准线抬到它的位置。这是一种**单向(只往右)**的贪心,不做 Vue 那种双端对比。

### 互动:lastPlacedIndex 追踪器(逐步看谁要移动)

**怎么操作：**先选择一种新顺序，再点“下一步”。每一步只看两个数字：当前节点的 `oldIndex` 与 `lastPlacedIndex`；前者更小时，该节点会标记为移动。

**惊人但真实的结论:把最后一个元素移到最前(D,A,B,C)会让 A、B、C 三个都被标记移动(3 次 DOM 移动);而把第一个移到最后(B,C,D,A)只移动 A 一个。** 因为 lastPlacedIndex 是「只往右」的贪心——它对「向后移动」友好、对「向前移动」很贵。所以做**列表头部插入 / 逆序**这类操作时,即使 key 正确,也可能产生大量 DOM 移动。这是 React Diff 一个真实的性能特性。

**React 18 优化点:**Diff 算法本身自 16 起就稳定(仍是上面这套两轮 + lastPlacedIndex)。18 的性能收益来自 Concurrent(把大 Diff 切片摊到多帧、可中断)+ 自动批处理(减少 Diff 次数),**不是改了 Diff 算法**。

### 📊 多节点 Diff 两轮遍历流程

<div className="tutorial-visual">
<svg viewBox="0 0 1000 300" style={{ width: "100%", height: "auto" }} fontFamily="ui-monospace,monospace"><defs><marker id="dfArr" markerWidth="9" markerHeight="9" refX="7" refY="3" orient="auto"><path d="M0,0 L7,3 L0,6 Z" style={{ fill: "var(--accent)" }} /></marker></defs><rect x="20" y="30" width="300" height="230" rx="12" style={{ fill: "rgba(126,231,135,.10)", stroke: "var(--recon)" }} strokeWidth="1.5" /><text x="170" y="55" textAnchor="middle" style={{ fill: "var(--recon)" }} fontSize="13" fontWeight="700">第一轮:从左到右同位比较</text> <rect x="45" y="75" width="250" height="40" rx="7" style={{ fill: "var(--panel)", stroke: "var(--line)" }} /><text x="170" y="100" textAnchor="middle" style={{ fill: "var(--txt)" }} fontSize="11.5">新旧同位置:key+type 相同?</text> <rect x="45" y="130" width="250" height="40" rx="7" style={{ fill: "rgba(97,218,251,.14)", stroke: "var(--accent)" }} /><text x="170" y="155" textAnchor="middle" style={{ fill: "var(--txt)" }} fontSize="11.5">相同 → 复用旧 fiber(改 props)</text> <rect x="45" y="185" width="250" height="50" rx="7" style={{ fill: "rgba(255,123,156,.14)", stroke: "var(--render)" }} /><text x="170" y="206" textAnchor="middle" style={{ fill: "var(--txt)" }} fontSize="11.5">一旦不同 → 立即跳出第一轮</text><text x="170" y="223" textAnchor="middle" style={{ fill: "var(--dim)" }} fontSize="10">(覆盖最常见的"尾部追加")</text> <line x1="325" y1="145" x2="375" y2="145" style={{ stroke: "var(--accent)" }} strokeWidth="2.5" markerEnd="url(#dfArr)" /><text x="350" y="135" textAnchor="middle" style={{ fill: "var(--dim)" }} fontSize="10">还有剩余</text> <rect x="380" y="30" width="600" height="230" rx="12" style={{ fill: "rgba(242,184,75,.10)", stroke: "var(--sched)" }} strokeWidth="1.5" /><text x="680" y="55" textAnchor="middle" style={{ fill: "var(--sched)" }} fontSize="13" fontWeight="700">第二轮:剩余旧节点建 Map(key→fiber)</text> <rect x="405" y="75" width="550" height="36" rx="7" style={{ fill: "var(--panel)", stroke: "var(--line)" }} /><text x="680" y="98" textAnchor="middle" style={{ fill: "var(--txt)" }} fontSize="11.5">遍历剩余新节点,用 key 去 Map 里捞</text> <rect x="405" y="123" width="175" height="55" rx="7" style={{ fill: "rgba(97,218,251,.14)", stroke: "var(--accent)" }} /><text x="492" y="145" textAnchor="middle" style={{ fill: "var(--txt)" }} fontSize="11">捞到 → 移动复用</text><text x="492" y="162" textAnchor="middle" style={{ fill: "var(--dim)" }} fontSize="9.5">lastPlacedIndex 判是否右移</text> <rect x="592" y="123" width="175" height="55" rx="7" style={{ fill: "rgba(126,231,135,.14)", stroke: "var(--recon)" }} /><text x="680" y="150" textAnchor="middle" style={{ fill: "var(--txt)" }} fontSize="11">捞不到 → 新建</text> <rect x="779" y="123" width="175" height="55" rx="7" style={{ fill: "rgba(255,123,156,.14)", stroke: "var(--render)" }} /><text x="866" y="145" textAnchor="middle" style={{ fill: "var(--txt)" }} fontSize="11">Map 里剩下的</text><text x="866" y="162" textAnchor="middle" style={{ fill: "var(--dim)" }} fontSize="9.5">→ 删除</text> <rect x="405" y="190" width="550" height="45" rx="7" style={{ fill: "none", stroke: "var(--line)" }} strokeDasharray="4 4" /><text x="680" y="217" textAnchor="middle" style={{ fill: "var(--dim)" }} fontSize="11">key 稳定 = 身份稳定 → 能"移动"而非"重建";index 当 key 会让身份错乱</text></svg>
</div>

**React 18 优化点:**Diff 本身稳定,18 的性能收益主要来自 Concurrent(可中断把大 Diff 摊到多帧)+ 自动批处理减少 Diff 次数,而非改 Diff 算法本身。

## 用 index 当 key 为什么出 bug(底层)

key 是节点的"身份证",Diff 靠它判断"这还是不是原来那个"。用数组 `index` 当 key:列表**头部插入**或**重排**时,同一个真实节点的 index 变了 → React 误判成"不同节点",把 **state / DOM 错配**到相邻项上(比如输入框内容串位),还可能退化成全量重建。

### 互动:index key vs id key 在头部插入时的差异

**怎么操作：**先保留 index key 点击“头部插入”，观察输入状态落到了哪个数据项；重置后切换为稳定 id，再执行一次进行对比。

**Render 的子节点协调完成：**Diff 决定哪些旧 Fiber 可以复用，哪些需要移动、插入或删除。下一章看 [变更标记](/02-update-pipeline/t10) 如何把这些决定记录下来，交给 Commit。

<TutorialSequence kind="diff" />

</div>
