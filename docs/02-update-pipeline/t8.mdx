---
title: Fiber：可中断工作单元
description: React 原理全景中的「Fiber：可中断工作单元」，从运行机制继续深入源码。
---

import { TutorialSequence } from '../../theme/TutorialSequence';

<div className="tutorial-chapter">

阶段三 · Render / 08

**🧭 主线**setState→Update→Lane→Scheduler→Render→Effect→Commit→DOM

**🗺 本章路线图**

Fiber 既是一种**数据结构**(带指针的对象),也是一个**可拆分、可中断的工作单元**。

本章包括：**① Fiber 字段含义 ② tag(节点类型)③ beginWork/completeWork 各干什么**(深度优先遍历)。

## Fiber 节点字段含义

```js
const fiber = {
  // —— 身份:我是谁 ——
  tag,            // 类型标记(FunctionComponent/HostComponent/HostRoot...)
  key,            // 元素的 key
  type,           // 元素类型(组件函数 / 'div')
  stateNode,      // 关联实例:DOM 节点 / class 实例 / fiberRoot
  // —— 链表指针:构成树,可暂停/恢复 ——
  return,         // 父 fiber
  child,          // 第一个子 fiber
  sibling,        // 下一个兄弟 fiber
  index,          // 在兄弟中的位置(多节点 Diff 用)
  // —— 工作数据 ——
  pendingProps,   // 新 props(本次待处理)
  memoizedProps,  // 上次渲染用的 props
  memoizedState,  // class:state / 函数:hooks 链表头
  updateQueue,    // 更新队列 / effect 链表
  // —— 调度 & 副作用 ——
  lanes,          // 本 fiber 的优先级车道
  childLanes,     // 子树是否有待处理更新(bailout 用)
  flags,          // 副作用标记 Placement/Update/Deletion...
  subtreeFlags,   // 子树副作用汇总(18 用它替代 effectList)
  deletions,      // 待删除的子 fiber 数组
  // —— 双缓存 ——
  alternate,      // 指向另一棵树里的自己
};
```

## tag:Fiber 到底是「哪种」节点

`tag` 表示 Fiber 的节点类型，`beginWork` 和 `completeWork` 会据此选择处理逻辑。常见取值如下：

tag

对应

说明

`HostRoot`(3)

根节点

整棵树的顶,stateNode 指向 fiberRoot

`FunctionComponent`(0)

函数组件

渲染 = 调用函数;hooks 挂 memoizedState

`ClassComponent`(1)

类组件

stateNode 是实例;state 挂 updateQueue

`HostComponent`(5)

原生标签(div/span)

stateNode 指向真实 DOM

`HostText`(6)

文本节点

纯文本

`Fragment` / `ContextProvider` / `SuspenseComponent` …

特殊节点

各有专门的 begin/complete 处理

括号里的数字（如 `HostRoot(3)`）是 `tag` 的实际值。`FunctionComponent`、`HostRoot` 和 `HostComponent` 是 `ReactWorkTags.js` 中定义的数字常量，因此源码可以通过 `switch(fiber.tag)` 区分节点类型。

## 工作单元:可拆分、可中断

每个 Fiber 就是一个"工作单元"。整棵树的协调 = 循环处理一个个工作单元,循环在单元之间可被 `shouldYield()` 打断(详见[第四层·Concurrent](/03-concurrent/t14))。

## beginWork 与 completeWork:各干一件事

Render 阶段主要通过下面两个函数遍历 Fiber 树：

### beginWork(往下)

**输入一个 fiber → 算出它的「子节点」。**

怎么算:如果是函数组件,就**调用你写的函数**拿到返回的 JSX;然后把新旧子节点做 Diff,生成子 fiber。

产物 = 子 fiber(带着往下走)。**它不碰真实 DOM。**

### completeWork(往上)

**输入一个 fiber → 为它造好「真实 DOM」。**

怎么造:`document.createElement` 建节点,把**已经造好的子 DOM** 塞进去,设置属性。

产物 = 一个离屏 DOM 节点(带着往上走)。

**「把新旧子节点做 Diff,生成子 fiber」到底啥意思?** 先分清两个东西:你 `return` 的 JSX(如 `&lt;h1&gt;hi&lt;/h1&gt;`)只是**这次想要的「描述」,还不是 fiber**;而**上次渲染**已经为它生成过对应的**旧子 fiber**(存在 current 树上,带着真实 DOM 和 state)。beginWork 拿**新描述**和**旧 fiber** 逐个对比(这就是 Diff):
· 新旧是同一个(key+type 相同)→ **复用旧 fiber**,只改属性。例:上次 `&lt;h1&gt;hi&lt;/h1&gt;`、这次 `&lt;h1&gt;bye&lt;/h1&gt;` → 还是 h1,复用那个 h1 fiber(连同它的 DOM),只把 hi 改成 bye,打个 Update;
· 变了(如 `&lt;h1&gt;` 变 `&lt;p&gt;`)→ 删掉旧的、新建一个;新增/减少同理。
**目的:能复用就复用,别每次把 fiber 和它的 DOM/state 全部重造。**产物就是这次的「子 fiber」(有的复用、有的新建)。**「复用」具体复用了什么(stateNode/memoizedState、createWorkInProgress)→ 见 [Render·双缓存](/02-update-pipeline/t5)。**

光看定义还是抽象,**连起来走一个最小例子**最清楚。渲染这个组件:

```js
渲染 function App(){ return <h1>hi</h1> }:

① beginWork(App)      // 调用 App() → 得到 <h1>hi</h1>,生成 h1 的 fiber   往下 ↓
② beginWork(h1)       // h1 的子是文本 "hi",生成文本 fiber              往下 ↓
③ beginWork("hi")     // 文本没有子节点 → 返回 null,到底了            到底
④ completeWork("hi")  // 创建文本节点 "hi"                          往上 ↑
⑤ completeWork(h1)    // 创建 <h1>,把 "hi" 塞进去 → <h1>hi</h1>    往上 ↑
⑥ completeWork(App)   // 函数组件没有真实 DOM,跳过                  往上 ↑
```

**看懂这张表就够了:**beginWork 一路**往下**,把 JSX 拆成一个个 fiber;completeWork 一路**往上**,把真实 DOM 一层层造出来、嵌套好。归到根节点时,一整棵 `&lt;h1&gt;hi&lt;/h1&gt;` 的**离屏 DOM** 就建好了,commit 时再一次性挂到页面。

点开：beginWork / completeWork 的源码骨架（读源码时看）

**1) 谁在驱动遍历**——`performUnitOfWork` 调 beginWork,拿到子节点就往下;没子节点就交给 `completeUnitOfWork` 往上走(靠 `sibling / return` 指针):

```js
function performUnitOfWork(wip){
  const next = beginWork(wip.alternate, wip, renderLanes);
  if(next === null) completeUnitOfWork(wip); // 没子节点 → 往上归
  else        workInProgress = next;              // 有子节点 → 往下递
}
function completeUnitOfWork(wip){
  let node = wip;
  do {
    completeWork(node.alternate, node, renderLanes);
    if(node.sibling){ workInProgress = node.sibling; return; } // 有兄弟 → 去兄弟
    node = node.return; workInProgress = node;                    // 没兄弟 → 回父节点
  } while(node);
}
```

**2) beginWork 先判断这个 Fiber 是否真的需要重新计算。**如果它的 props、state 和 context 都没有需要处理的变化，React 会直接复用上一次的 Fiber 子树，不再执行组件和 Diff。源码把这种“确认没变化后提前退出”的优化叫作 `bailout`。

**可以先把 bailout 理解成“这条分支没变，跳过”。**如果不能跳过，`beginWork` 才会根据 `tag` 判断 Fiber 类型：函数组件就执行函数和 Hooks，类组件就调用 `render`，原生标签就读取 `props.children`；随后通过 `reconcileChildren` 比较子节点。

```js
function beginWork(current, wip, renderLanes){
  switch(wip.tag){
    case FunctionComponent: return updateFunctionComponent(…); // renderWithHooks:调你的函数、跑 Hooks
    case ClassComponent:    return updateClassComponent(…);    // 调 instance.render()
    case HostRoot:          return updateHostRoot(…);          // processUpdateQueue 取 <App/>
    case HostComponent:     return updateHostComponent(…);     // 取 props.children
  } // 每个分支最后都 reconcileChildren(做 Diff)并 return wip.child
}
```

**3) completeWork 内部**:首次挂载建 DOM 并把子 DOM 拼进去;更新则算属性差异打 Update;最后 `bubbleProperties` 把子树副作用冒泡进 `subtreeFlags`(即[Render·变更标记](/02-update-pipeline/t10)的收集):

```js
function completeWork(current, wip){
  switch(wip.tag){
    case HostComponent:
      if(current && wip.stateNode){
        updateHostComponent(current, wip, …);   // 更新:diffProperties 算差异,打 Update
      } else {
        const dom = createInstance(type, props); // 首次:建真实 DOM
        appendAllChildren(dom, wip);             // 把已造好的子 DOM 塞进去
        finalizeInitialChildren(dom, type, props); // 设初始属性
        wip.stateNode = dom;
      }
      break;
  }
  bubbleProperties(wip); // 汇总子树 flags → subtreeFlags
}
```

### 互动:单步走一遍 递→归 遍历

**怎么操作：**连续点击“下一步”。绿色表示 `beginWork` 向子节点前进，粉色表示 `completeWork` 完成节点并返回父级；只需跟随当前高亮节点。

绿 = beginWork(递·往下) 粉 = completeWork(归·往上)

**Render 的执行框架：**`child/sibling/return` 让 React 逐个处理 Fiber 工作单元；`beginWork` 向下计算，`completeWork` 向上完成。下一章看 [双缓存](/02-update-pipeline/t5) 如何保存 current 与 workInProgress 两个版本。

<TutorialSequence kind="fiber" />

</div>
