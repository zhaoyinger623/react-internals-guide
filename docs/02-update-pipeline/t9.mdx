---
title: UpdateQueue：更新入队
description: React 原理全景中的「UpdateQueue：更新入队」，从运行机制继续深入源码。
---

import { TutorialSequence } from '../../theme/TutorialSequence';

<div className="tutorial-chapter">

阶段一 · Trigger / 05

**🧭 主线**setState→Update→Lane→Scheduler→Render→Effect→Commit→DOM

**🗺 本章路线图**

一个组件 = 一个 Fiber = 一个 UpdateQueue;setState 把更新排进队列,渲染时一起 reduce 出最终 state。

本章包括：**① update 长啥样 ② 怎么入队(环状链表)③ 怎么算(processUpdateQueue)④ baseState/baseQueue(并发存档)**。

每次状态更新都会进入对应的更新队列。不过，**类组件/HostRoot 与 `useState` Hook 使用的队列字段并不相同**。它们的共同目标都是：保存新更新、按插入顺序计算，并在跳过低优先级更新后保留重算所需的信息。

## ① 先区分类组件队列与 Hook 队列

```js
// 一个 Update 对象(class 组件的形态;hook 版字段略有差异)
const update = {
  lane,              // 本次更新的优先级车道
  payload,           // setState 传的对象,或 (state)=>newState
  next,              // → 下一个 update(环状链表)
};
```

### 类组件与 HostRoot

```js
fiber.updateQueue = {
  baseState,                  // 计算的「起点」state
  firstBaseUpdate,            // 基础队列的头
  lastBaseUpdate,             // 同一队列的尾
  shared: { pending: null },  // 新进入的环状队列
}
```

`firstBaseUpdate` 与 `lastBaseUpdate` 不是两个队列，而是**同一条单向链表的头、尾两个指针**。

### useState / useReducer Hook

```js
hook = {
  baseState,
  baseQueue,                  // 基础环状队列的尾指针
  queue: {
    pending: null             // 新进入的环状队列
  }
}
```

Hook 没有 `firstBaseUpdate/lastBaseUpdate`；它用一个 `baseQueue` 尾指针表示基础环状队列，队头同样是 `baseQueue.next`。

**为什么类组件要同时保存 first 和 last？**处理队列时要从 first 开始向后遍历；合并新 update 时又要快速找到 last 追加。只有 first，追加时需要遍历整条链表；同时保存 last，追加就是 O(1)。

## ② 新 update 先进入 pending 环状链表

下面先看类组件的 `shared.pending`；Hook 的 `queue.pending` 插入方式相同。多次更新形成环状链表，`pending` 指向最后一个节点，`pending.next` 指向第一个节点。

**先把范围说准确：**“pending 环转成普通链表”特指**类组件与 HostRoot**。Hook 的 `queue.pending` 也是环，但它会与同为环状的 `baseQueue` 合并，处理时仍可用“记住队头，绕一圈回到队头即结束”的方式遍历。

### 为什么同一批 update 要切换两种形态

因为这批 update 在进入 Render 前后承担的是两种完全不同的任务。可以把 `pending` 看成**收件箱**，把基础队列看成 Render 的**工作清单**：

所处阶段

主要操作

适合的形态

为什么

**事件发生时：收集**

不断把新 update 追加到末尾

尾指针表示的环

只存 `pending` 一个指针，就能用 `pending` 找队尾、用 `pending.next` 找队头；每次追加都是 O(1)。此时不需要遍历或计算 state。

**Render 开始后：处理**

从最早的 update 向后计算，并把跳过的 update 保存成新的基础队列

类组件使用普通单链表

从 `firstBaseUpdate` 一路走到 `null` 即结束；同时用 `lastBaseUpdate` 快速追加，适合顺序消费和重建基础队列。

### 环转普通链表时，具体只发生四件事

1.  **先保存队尾：**`last = shared.pending`，例如得到 u3。
2.  **在断环前保存队头：**`first = last.next`，因此仍能找到 u1。
3.  **断开队尾到队头的回边：**`last.next = null`，于是 `u1 → u2 → u3 → u1` 变为 `u1 → u2 → u3 → null`。
4.  **接入工作清单：**把 `first/last` 追加到类组件基础队列的 `firstBaseUpdate/lastBaseUpdate` 后面。

**为什么一定要先读 `last.next` 再断环？**因为外部只保存了队尾 `last`，队头藏在 `last.next` 中。如果先执行 `last.next = null`，就会丢失队头入口，无法找到 u1。正确顺序是“先取队头，再断开回边”。

所以，环并不是“计算 update 更方便”，普通链表也不是“入队更快”。React 是在每个阶段选择更适合当下操作的表示：**环负责高频收集和整批移交，普通链表负责有明确终点的顺序计算。**

### 为什么收集阶段选择尾指针环

pending 可能连续插入很多次，但一轮 Render 通常只需接管这个入口。环状结构让收集阶段只保存一个 `pending` 指针，却能同时 O(1) 找到队尾和队头：

结构

如何找队头

如何找队尾

尾部插入

需要几个外部指针

普通单链表，只存 head

`head`

必须从头遍历

O(n)

1

普通单链表，存 head + tail

`head`

`tail`

O(1)

2

**环状单链表，只存 tail**

`tail.next`

`tail`

O(1)

**1**

**环的收益发生在入队阶段：**假设 Render 前来了 100 个 update，React 会做 100 次 O(1) 入队；Render 接管时只做一次 O(1) 的断环。总成本仍是 O(n)，但不会出现“第 1 次找 0 个节点、第 2 次找 1 个节点……第 100 次找 99 个节点”的反复尾部遍历。

**`null` 终点为什么适合类组件的处理过程？**`processUpdateQueue` 不只是读取旧队列，还会在遇到低优先级 update 后构造一条新的基础队列。普通链表可以用 `update !== null` 控制遍历，并用新的 first/last 指针逐个追加需要保留的节点；无需额外保存“绕一圈后的停止哨兵”。

普通的 head + tail 双指针链表也能实现 O(1) 入队；环状链表不是唯一方案。React 的选择是用一个尾指针同时编码头和尾，并让 Hook 的 pending 环与 baseQueue 环可以通过少量指针修改完成合并。

### 连续插入 u1、u2、u3 后，指针怎样变化

<div className="tutorial-visual">
<svg viewBox="0 0 900 430" style={{ width: "100%", height: "auto" }} role="img" ariaLabelledby="uqRingTitle uqRingDesc"><title id="uqRingTitle">UpdateQueue 环状链表插入过程</title> <desc id="uqRingDesc">pending 始终指向最后插入的 update，pending.next 始终指向第一个 update。</desc> <defs><marker id="uqArrow" markerWidth="9" markerHeight="9" refX="7" refY="3" orient="auto"><path d="M0,0 L7,3 L0,6 Z" style={{ fill: "var(--accent)" }} /></marker><marker id="uqPendingArrow" markerWidth="9" markerHeight="9" refX="7" refY="3" orient="auto"><path d="M0,0 L7,3 L0,6 Z" style={{ fill: "var(--sched)" }} /></marker></defs><g fontFamily="ui-monospace,monospace"><text x="20" y="28" style={{ fill: "var(--strong)" }} fontSize="13" fontWeight="700">① 插入 u1：第一个节点自己指向自己</text> <rect x="365" y="48" width="120" height="48" rx="10" style={{ fill: "var(--panel2)", stroke: "var(--accent)" }} strokeWidth="2" /><text x="425" y="78" textAnchor="middle" style={{ fill: "var(--txt)" }} fontSize="13">u1</text> <path d="M485,72 C555,72 555,120 425,120 C335,120 335,92 363,78" fill="none" style={{ stroke: "var(--accent)" }} strokeWidth="2" markerEnd="url(#uqArrow)" /><text x="515" y="112" style={{ fill: "var(--dim)" }} fontSize="10.5">u1.next → u1</text> <path d="M235,72 L360,72" fill="none" style={{ stroke: "var(--sched)" }} strokeWidth="2" markerEnd="url(#uqPendingArrow)" /><text x="80" y="77" style={{ fill: "var(--sched)" }} fontSize="11.5">pending → u1</text> <line x1="20" y1="145" x2="880" y2="145" style={{ stroke: "var(--line)" }} /><text x="20" y="172" style={{ fill: "var(--strong)" }} fontSize="13" fontWeight="700">② 插入 u2：把 u2 接到尾部，pending 改指 u2</text> <rect x="245" y="195" width="120" height="48" rx="10" style={{ fill: "var(--panel2)", stroke: "var(--accent)" }} strokeWidth="2" /><text x="305" y="225" textAnchor="middle" style={{ fill: "var(--txt)" }} fontSize="13">u1 · 队头</text> <rect x="535" y="195" width="120" height="48" rx="10" style={{ fill: "var(--panel2)", stroke: "var(--sched)" }} strokeWidth="2" /><text x="595" y="225" textAnchor="middle" style={{ fill: "var(--txt)" }} fontSize="13">u2 · 队尾</text> <path d="M365,219 L530,219" fill="none" style={{ stroke: "var(--accent)" }} strokeWidth="2" markerEnd="url(#uqArrow)" /><text x="447" y="210" textAnchor="middle" style={{ fill: "var(--dim)" }} fontSize="10.5">u1.next</text> <path d="M595,245 C595,280 305,280 305,247" fill="none" style={{ stroke: "var(--accent)" }} strokeWidth="2" markerEnd="url(#uqArrow)" /><text x="450" y="274" textAnchor="middle" style={{ fill: "var(--dim)" }} fontSize="10.5">u2.next → u1（回到队头）</text> <path d="M700,184 L650,204" fill="none" style={{ stroke: "var(--sched)" }} strokeWidth="2" markerEnd="url(#uqPendingArrow)" /><text x="706" y="177" style={{ fill: "var(--sched)" }} fontSize="11.5">pending → u2</text> <line x1="20" y1="300" x2="880" y2="300" style={{ stroke: "var(--line)" }} /><text x="20" y="327" style={{ fill: "var(--strong)" }} fontSize="13" fontWeight="700">③ 插入 u3：只改尾部的两条指向</text> <rect x="135" y="350" width="120" height="48" rx="10" style={{ fill: "var(--panel2)", stroke: "var(--accent)" }} strokeWidth="2" /><text x="195" y="380" textAnchor="middle" style={{ fill: "var(--txt)" }} fontSize="13">u1 · 队头</text> <rect x="390" y="350" width="120" height="48" rx="10" style={{ fill: "var(--panel2)", stroke: "var(--accent)" }} strokeWidth="2" /><text x="450" y="380" textAnchor="middle" style={{ fill: "var(--txt)" }} fontSize="13">u2</text> <rect x="645" y="350" width="120" height="48" rx="10" style={{ fill: "var(--panel2)", stroke: "var(--sched)" }} strokeWidth="2" /><text x="705" y="380" textAnchor="middle" style={{ fill: "var(--txt)" }} fontSize="13">u3 · 队尾</text> <path d="M255,374 L385,374 M510,374 L640,374" fill="none" style={{ stroke: "var(--accent)" }} strokeWidth="2" markerEnd="url(#uqArrow)" /><path d="M705,400 C705,425 195,425 195,402" fill="none" style={{ stroke: "var(--accent)" }} strokeWidth="2" markerEnd="url(#uqArrow)" /><text x="450" y="422" textAnchor="middle" style={{ fill: "var(--dim)" }} fontSize="10.5">u3.next → u1（环闭合）</text> <path d="M805,338 L760,358" fill="none" style={{ stroke: "var(--sched)" }} strokeWidth="2" markerEnd="url(#uqPendingArrow)" /><text x="770" y="327" style={{ fill: "var(--sched)" }} fontSize="11.5">pending → u3</text></g></svg>
</div>

**最终只要记住两个入口：**`pending` 是队尾 u3；`pending.next` 是队头 u1。遍历从 u1 开始，依次经过 u2、u3，再回到 u1 时结束。

```js
function enqueueUpdate(fiber, update){
  const queue = fiber.updateQueue.shared;
  const pending = queue.pending;
  if(pending === null) update.next = update;    // 第一个:自己成环
  else { update.next = pending.next;             // 新节点接到头前
         pending.next = update; }
  queue.pending = update;   // pending 永远指向「最后进来的」
}
```

**把源码代入“插入 u3”：**
进入函数前：`pending = u2`，`pending.next = u1`。
① `u3.next = pending.next`，所以 `u3.next = u1`；
② `pending.next = u3`，所以 `u2.next = u3`；
③ `queue.pending = u3`，队尾指针从 u2 移到 u3。
旧的 `u1.next = u2` 不需要修改，最终得到 `u1 → u2 → u3 → u1`。

### `pending` 还划出了一批更新的边界

`shared.pending` 不只是“指向队尾”。它还代表**尚未被 Render 接管的整批新更新**。Render 开始处理类组件队列时，会先取走这个指针并把 `shared.pending` 清空：

```js
// Render 开始：把当前 pending 整批交给本轮计算
const lastPending = queue.shared.pending;  // u3（队尾）
queue.shared.pending = null;              // pending 槽位重新空出来

const firstPending = lastPending.next;      // u1（队头）
lastPending.next = null;                  // u1 → u2 → u3 → null

// 再把 firstPending...lastPending 接到基础队列尾部
```

**Render 接管前**

`shared.pending → u3`
`u1 → u2 → u3 → u1`

→

**取走并清空 pending**

本轮持有 `u1～u3`
`shared.pending = null`

→

**之后又收到 u4**

`u4.next = u4`
`shared.pending → u4`

**这就是“批次边界”：**React 只交换一个 `pending` 指针，就把 u1～u3 整批交给当前处理流程；之后到来的 u4 会在空出来的 `pending` 中重新成环，不会破坏已经拆出的链表。如果本轮处理结束前又发现了这批更新，React 可以继续把它接进来；否则由后续工作处理。

### Hook 为什么也保留环：两个队列可以 O(1) 拼接

Hook 的 `baseQueue` 和 `queue.pending` 都是“尾指针 + 环”。假设旧基础队列是 `b1 → b2 → b1`，新 pending 是 `p1 → p2 → p1`。React 已经同时拿到了两个队尾，因此只要交换两条连接：

```js
const baseFirst = baseLast.next;        // b1
const pendingFirst = pendingLast.next;  // p1

baseLast.next = pendingFirst;               // b2 → p1
pendingLast.next = baseFirst;               // p2 → b1，重新闭环
baseQueue = pendingLast;                     // 新队尾是 p2
```

**合并后：**`b1 → b2 → p1 → p2 → b1`。旧更新仍在前，新更新仍在后；无论两个环里各有多少节点，合并都不需要遍历，所以仍是 O(1)。这比“环能少存一个指针”更重要：**它让 React 能快速接管、拼接和保留整批更新。**

## ③ pending 与基础队列按什么顺序执行

`pending` 是“渲染开始前新收到的更新”，基础队列是“本次必须从哪里继续计算”。开始处理类组件队列时，React 会先把 pending 环拆开并追加到基础队列末尾，然后从头按插入顺序遍历。

**渲染开始前**

基础队列：`b1 → b2`
pending 环：`p1 → p2 → p1`

→

**拆开 pending 环**

`p2.next = null`
得到 `p1 → p2`

→

**追加到基础队列**

`b1 → b2 → p1 → p2`

→

**从 first 开始计算**

执行顺序固定为：
`b1、b2、p1、p2`

字段

处理前

处理过程中

处理后

`shared.pending`

保存刚进入的环状 update

环被拆开并接到基础队列末尾

清空为 `null`；之后新来的 update 会形成下一批 pending

`firstBaseUpdate`

指向基础队列第一个节点

从这里开始按 `next` 遍历

若没有跳过更新则为 `null`；否则指向新基础队列的第一个节点

`lastBaseUpdate`

指向基础队列最后一个节点

用于 O(1) 追加 pending

若没有跳过更新则为 `null`；否则指向新基础队列的最后一个节点

**Hook 的顺序相同，只是链表形态不同：**`hook.baseQueue` 和 `queue.pending` 都用尾指针表示环状链表。Render 开始时，React 先把 pending 环接到旧 baseQueue 环的末尾，再从 `baseQueue.next`（队头）按插入顺序处理。没有旧 baseQueue 时，pending 直接成为本次基础队列。

## ④ 渲染时怎么算:从 baseState 逐个 reduce(processUpdateQueue)

渲染时 React 调用一个叫 `processUpdateQueue` 的函数来算最终 state:**从 `baseState`(起点值)出发,按顺序把队列里每个 update 应用一遍**(像滚雪球),得到本次的 `memoizedState`。这就是「多次 setState 被合并成一次结果」:

```js
// processUpdateQueue 概念:从 baseState 开始,依次 apply 每个 update
let newState = queue.baseState;
for(const u of 队列里的 update){
  newState = reduce(newState, u);   // u 是对象则合并;是函数则 u(newState)
}
fiber.memoizedState = newState;      // 得到本次最终 state
```

所以 `setCount(c=>c+1)` 连写三次会累加(每个 update 拿到前一个的结果),而 `setCount(count+1)` 连写三次只 +1(闭包里 count 是同一个旧值)。

## ⑤ 为什么需要 baseState 和基础队列

先不要记字段名。只看同一个 `count` 按顺序收到的三次更新：

`u1: c => c + 1`（高优） → `u2: c => c + 10`（低优） → `u3: c => c * 2`（高优）

初始值是 `0`。React 先处理高优更新时，会按原顺序走一遍：

1.  **执行 u1：**`0 + 1 = 1`。
2.  **暂时跳过 u2：**它是低优更新。这一刻的值 `1` 被记为 `baseState`，意思是“下次补算要从这里开始”。
3.  **执行 u3：**`1 × 2 = 2`，所以本次可以先显示 `2`。

问题是：以后补算 u2 时，不能只算 `1 + 10 = 11`。原始顺序是 `u2 → u3`，所以还要再算一次 u3，最终才是 `(1 + 10) × 2 = 22`。

**基础队列就是“下次必须重放的那一段顺序”。**从第一个被跳过的 u2 开始，React 保存 `[u2, u3]`。u3 本次虽然已经执行，仍要留一份副本，保证下次能恢复原来的计算顺序。

下面把这两次 Render 展开。先看数值如何变化，再对应 `baseState` 和基础队列：

**📽 一步步看:初始 `state = 0`,队列里三个 update**

u1: c=>c+1 高优 u2: c=>c+10 低优 u3: c=>c\*2 高优

▼ 本次渲染(renderLanes = 高优)

① u1: c=>c+1高优应用state **0 → 1**baseState=0 · baseQueue=\[\]

② u2: c=>c+10低优跳过 ✋state **1**(不变)⚠ 第一次跳过 → baseState=**1** · baseQueue=\[**u2**\]

③ u3: c=>c\*2高优应用 + 也要存档state **1 → 2**前面跳过了,u3 也进 → baseQueue=\[u2, **u3**\]

**本次屏幕显示 state = 2**

▼ 下次渲染(轮到低优)——从存档点重放

从 baseState=**1** 出发 → 重放 baseQueue u2:+10 → 11 → u3:\*2 → \= 22 ✅

**(1 + 10) × 2 = 22**,最终 state 正确。屏幕先显示 2(高优先给用户看),真正的最终值 22 由下次低优渲染补上。

**现在再记定义：**`baseState` 是第一次跳过更新时已经算出的 state；基础队列是从该更新开始、下次需要按顺序重放的 update。这里分别是 `1` 和 `[u2, u3]`。

**为什么 u3 的副本使用 `NoLane`？**因为 u3 已经通过了本次的优先级检查。下次重放时，它必须跟在 u2 后面执行，不能再因 Lane 不匹配而被跳过；`NoLane` 在这里表示“这个副本重放时直接执行”。

**“无关的更新”要分情况：**另一个 `useState` 有自己的队列，不会混进来；但同一个 state 队列里的后续 update 都要保留顺序。React 不会猜测两个更新函数在业务语义上是否互不影响。

源码中，`isSubsetOfLanes(renderLanes, update.lane)` 决定本次是否跳过 update。类组件用 `firstBaseUpdate/lastBaseUpdate` 保存这段基础队列；State Hook 用 `hook.baseQueue` 保存。

### 互动:shared.pending 环状链表 + 优先级跳过

**怎么操作：**按“高优 → 低优 → 高优”加入三个 update。点击①，观察第 3 个高优 update 本次执行但仍留下副本；再点击②，观察基础队列按原顺序重放后清空。

**Trigger 阶段完成：**`setState` 产生 update，并将它放入更新队列。下一步进入 Schedule：由 [Lane](/02-update-pipeline/t13) 标记和选择更新优先级。

<TutorialSequence kind="queue" />

</div>
